<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <style>
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap');
</style>

<nav class="bg-gray-100">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <a href="{{path('app_home')}}" class="text-gray-800 font-bold text-xl" style="font-family: 'Poppins', sans-serif;">AI App</a>
        </div>
        <div class="hidden md:block">
          <div class="ml-10 flex items-baseline space-x-4">
            <a href="{{path('app_home')}}" class="text-gray-600 hover:text-gray-800 px-3 py-2 rounded-md text-sm font-medium" style="font-family: 'Poppins', sans-serif;">Accueil</a>
            <a href="#" class="text-gray-600 hover:text-gray-800 px-3 py-2 rounded-md text-sm font-medium" style="font-family: 'Poppins', sans-serif;">Services</a>
            <a href="#" class="text-gray-600 hover:text-gray-800 px-3 py-2 rounded-md text-sm font-medium" style="font-family: 'Poppins', sans-serif;">À Propos</a>
            <a href="#" class="text-gray-600 hover:text-gray-800 px-3 py-2 rounded-md text-sm font-medium" style="font-family: 'Poppins', sans-serif;">Contact</a>
          </div>
        </div>
      </div>
      <div class="hidden md:block">
        <div class="flex items-center">
          <input type="text" class="px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Rechercher..." style="font-family: 'Poppins', sans-serif;">
        </div>
      </div>
      <div class="hidden md:block">
        <div class="ml-4 flex items-center md:ml-6">
          <button class="bg-gray-300 p-1 rounded-full text-gray-600 hover:text-gray-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-white" style="font-family: 'Poppins', sans-serif;">
            <span class="sr-only">View notifications</span>
            <!-- Ajoutez ici une icône de notifications -->
          </button>
        {% if app.user %}
        <div class="flex items-center">
        <img src="{{ app.user.avatar }}" alt="Avatar" class="w-10 h-10 rounded-full">
        <div class="ml-4">
            <h3 class="text-sm font-medium">{{ app.user.firstName }}</h3>
            <p class="text-xs text-gray-500">{{ app.user.email }}</p>
            <p class="text-xs text-gray-500"> <a href="{{path('app_logout')}}" class="text-gray-600">Déconnexion</a></p>
        </div>
        </div>
        </div>
        {% else %}
        <div class="bg-blue-500 text-white font-bold px-4 py-2 rounded-md">
            <a href="{{path('app_login')}}" class="text-white">Connexion</a>
        </div>

        <div class="bg-green-500 text-white font-bold px-4 py-2 rounded-md">
            <a href="{{path('app_logout')}}" class="text-white">Inscription</a>
        </div>
        {% endif %}
        </div>
      </div>
    </div>
  </div>
</nav>

    <body>
        {% block body %}{% endblock %}
    </body>
</html>
